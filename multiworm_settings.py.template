import pathlib

#MWT_DATA_ROOT = pathlib.Path('/home/projects/worm_movement/Data/MWT_RawData')
MWT_DATA_ROOT = (
    pathlib.Path(__file__).parent / 
    '..' / # shared
    '..' / # code
    '..' / # Waldo
    'data' / 
    'mwt').resolve()
